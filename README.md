# M2PI
A RAG-based FAQ support chatbot that indexes internal documentation into vectorized chunks and answers user questions by retrieving the most relevant context before generating responses using an LLM. It uses OpenAI embeddings and GPT-4o-mini for generation, returning auditable JSON outputs with the question, answer, and the chunks used as source.
